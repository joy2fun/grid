
create a table named grids to store grid trading strategies plans with the following colums:
 - friendly name
 - stock id related to stocks table
 - grid_interval/initial_amount or any columns that is necessary for grid trading strategies

create a table named trades to record all the trading operations for all stocks. maybe a column linking to the grids table. come up with any resonable columns if necessary 

run the migration command.

create filament resource pages to manage these grids and tradings operations. their list pages should sort by id in desc order.
